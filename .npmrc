# Enable shamefully-hoist only if needed for specific packages
shamefully-hoist=false

# Strict peer dependencies to catch version conflicts
strict-peer-dependencies=false

# Auto install peers to handle React ecosystem
auto-install-peers=true
